{% extends 'base.html.twig' %}

{% block title %}Unity{% endblock %}

{% block body %}
    <h1>Unity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unity.name }}</td>
            </tr>
            <tr>
                <th>Symbol</th>
                <td>{{ unity.symbol }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unity_index') }}">back to list</a>

    <a href="{{ path('unity_edit', {'id': unity.id}) }}">edit</a>

    {{ include('unity/_delete_form.html.twig') }}
{% endblock %}
