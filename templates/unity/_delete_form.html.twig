<form method="post" action="{{ path('unity_delete', {'id': unity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unity.id) }}">
    <button class="btn">Delete</button>
</form>
